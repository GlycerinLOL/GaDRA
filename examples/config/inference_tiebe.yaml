# TiEBe (retention) — GPT-judged "Correct %" (`python -m examples.inference --config <this>`).
# Needs OPENAI_API_KEY. max_new_tokens=128 (other tasks use 256).
# base + adapter come from the suite (inference.yaml) or `--override adapter=...`.
chat_template_path: examples/config/llama3.2-Instruct.jinja

task: tiebe
eval_file: data/TiEBe/pre_2023/world.jsonl
use_raw_text: false                             # prompt built from `messages` via chat template
batch_size: 32
max_new_tokens: 128                             # TiEBe-specific; other tasks use 256

# GPT judge (key from OPENAI_API_KEY env var):
gpt_model: gpt-4.1-mini
gpt_repeats: 1
gpt_vote_rate: 50
gpt_workers: 32                                 # parallel judge calls
